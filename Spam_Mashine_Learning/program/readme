各プログラムは統計解析用のものです。
R言語を実行するにあたり、元の解析データ、週ごと、日ごと、月ごとの視点を持って解析していくことが必要です。

祝日効果,R特有のデータ整形などを鑑みてプログラムを作成しました。

day.pl 日ごとの集計です。
week.pl 週ごとの集計です（月、火、水、木などのものではありません。）
month　月ごとの集計です。（2009年に日付を合わせてありますので、集計数を増やしたいと思えば@month月の日付をいじる必要があります。）

